<!-- Empty username and password -->
<storeProperty name="username" value=""/>
<storeProperty name="password" value=""/>
&goToLoginPage;
&submitLoginData;
<verifytitle description="we should see the login title yet" text=".*${login.title}.*" regex="true"/>
<verifytext description="Username is a required field" text=".*${label.username} ${error.required_field}.*" regex="true"/>
<verifytext description="Password is a required field" text=".*${label.password} ${error.required_field}.*" regex="true"/>

<!-- Empty username -->
<storeProperty name="username" value="aaaa"/>
<storeProperty name="password" value=""/>
&goToLoginPage;
&submitLoginData;
<verifytitle description="we should see the login title yet" text=".*${login.title}.*" regex="true"/>
<verifytext description="Password is a required field" text=".*${label.password} ${error.required_field}.*" regex="true"/>

<!-- Empty password -->
<storeProperty name="username" value=""/>
<storeProperty name="password" value="aaaa"/>
&goToLoginPage;
&submitLoginData;
<verifytitle description="we should see the login title yet" text=".*${login.title}.*" regex="true"/>
<verifytext description="Username is a required field" text=".*${label.username} ${error.required_field}.*" regex="true"/>

<!-- Incorrect username or password -->
<storeProperty name="username" value="aaaa"/>
<storeProperty name="password" value="aaaa"/>
&goToLoginPage;
&submitLoginData;
<verifytitle description="we should see the login title yet" text=".*${login.title}.*" regex="true"/>
<verifytext description="Either username or password is incorrect" text=".*${BindAuthenticator.badCredentials}.*" regex="true"/>