En esta última iteración del ciclo se han corregido algunos \textit{bugs} que se han detectado a lo largo del ciclo de desarrollo. Además, tal y como se indica en la ficha de iteración \ref{table:ficha-iteracion-10} se elaboran los documentos asociados al proyecto:
\begin{itemize}
	\item \textbf{Manual de instalación y despliegue}. Documento que indica cómo construir los proyectos y pasar la herramienta a producción. será vital para el equipo que se encargue del mantenimiento del sistema (Véase Anexo \ref{ch:manualinstalacion}).
	\item \textbf{Manual de usuario}. Documento que explica las principales funcionalidades de las que dispone el sistema desarrollado (Véase Anexo \ref{ch:manualdeusuario}).
\end{itemize}

A continuación se procede la creación del paquete de distribución y su instalación en la empresa. Este proceso consta de dos partes, una para cada componente:
\begin{itemize}
	\item \textbf{Motor gráfico}. Este componente será distribuido en un paquete JAR (Java ARchive). Gracias al uso de Maven para la gestión del ciclo de vida del proyecto, basta con ejecutar el comando \textit{mvn package} para obtener el correspondiente archivo. A continuación, se colocará el fichero JAR obtenido bajo el directorio \textit{src\textbackslash{}main\textbackslash{}webapp\textbackslash{}applet} con el nombre \textit{desglosa.jar}.
	\item \textbf{Aplicación web}. Este componente será distribuido en formato WAR (Web application ARchive), que contiene todo el código ejecutable de la aplicación web. Una vez más, se ejecutará el comando \textit{mvn package} para obtener el fichero correspondiente, que deberá ser desplegado en un contenedor de servlets y páginas JSP.
\end{itemize}

Para finalizar, cabe destacar la posibilidad de la generación automática de páginas web para la distribución de cada uno de los componentes que conforman este PFC. Esto es posible, una vez más, gracias al uso de Maven. Para generar dichas páginas web bastará con ejecutar el comando \textit{mvn site} para cada uno de los componentes.
