<storeProperty name="username" value="locked" />
<storeProperty name="password" value="locked" />
&goToLoginPage;
&submitLoginData;
<verifytitle description="we should see the login title yet" text=".*${login.title}.*" regex="true"/>
<verifytext description="User account must be locked" text=".*${UserDetailsService.locked}.*" regex="true"/>