<config host="${host}"
		port="${port}"
		protocol="http"
    	basepath="${webapp.name}"
    	resultpath="target/webtest/webtest-results"
    	saveresponse="true"
    	summary="true">
    <header name="Accept-Language" value="${user.language}"/>
</config>